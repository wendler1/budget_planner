"use strict";


// Get the input from the user
let title_1 = prompt("title:");
console.log(title_1);

let input_1 = prompt("input (Einnahme/Ausgabe):");
console.log(input_1);

let amount = prompt("amount (in Cent): ");
console.log(amount);

let date = prompt("date (yyyy-mm-dd):");
console.log(date);

// Overall balance
let income;
let outcome;
let total;
